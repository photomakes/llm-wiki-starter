# Память проекта demo-project — индекс

> Индекс загружается в контекст каждой сессии — одна строка на факт, без
> содержимого. Само содержимое — в отдельных файлах (1 факт = 1 файл).
> Агент читает индекс первым и по нему решает, в какой файл заглянуть.

- [prefers-short-answers.md](prefers-short-answers.md) — feedback: сначала короткий вывод, детали по запросу.
